{
  "name": "shiny",
  "language": "R",
  "app": {
    "code": [
      "'library(htmltools);",
      "shiny::shinyAppTemplate(path, examples = \\\"all\\\", dryrun = FALSE)'"
    ]
  },
  "module": {
    "code": null,
    "file": {
      "filename": "module.R",
      "content": [
        "ModuleUI <- function(id, label = \"Counter\") {",
        "  # All uses of Shiny input/output IDs in the UI must be namespaced,",
        "  # as in ns(\"x\").",
        "  ns <- NS(id)",
        "}",
        "",
        "ModuleServer <- function(id) {",
        "  # moduleServer() wraps a function to create the server component of a",
        "  # module.",
        "  moduleServer(id, function(input, output, session) {",
        "",
        "  })",
        "}"
      ]
    }
  },
  "addins": []
}